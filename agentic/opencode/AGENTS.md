# Requirements:
1. Update AGENTS.md for important things that must be remembered.
2. Once you have shown your plan to user, do not change to alternative solution unless user agrees, always confirm with the user. If the solution doesn't work, just say it, don't be "smart".
3. If you are completing programming tasks, always check if project compiles if you have modified the code.
4. For java project, always add `@author yongj.zhuang` if the class is newly created.
5. For golang project, always use `go fmt ./...` for formatting.
6. When calculating numbers, ALWAYS use python scripts to compute the results, NEVER CALCULATE YOURSELF!
7. Always response with Chinese unless I ask you to use another language specifically.